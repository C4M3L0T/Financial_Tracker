# Copia este archivo a .env (gitignorado) en esta misma carpeta y pon
# contraseñas reales. MARIADB_PASSWORD es la del usuario 'arch' — la misma
# que va como DB_PASSWORD en el config.py de cada cliente y del bot.
MARIADB_ROOT_PASSWORD=cambia-esta-contrasena-root
MARIADB_PASSWORD=cambia-esta-contrasena
